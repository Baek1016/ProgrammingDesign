7. 두 수를 매개변수로 받아 각각 최대값과 최소값을 반환하는 max()와 min() 함수를 작성하시오.(5점)
8. 다음과 같은 html에서 text 입력 상자를 선택하기 위한 3가지 방법의 javascript 코드를 작성하시오.(9점)

Chap6
인라인과 블록 요소가 먼지 설명후 예제를 최대한 많이 적으시오
블록 요소=화면의 한줄을 전부 차지한다.
<h1>, <p>, <ul>, <li>, <table>, <blockquote>, <pre>, <div> <form> , <header>, <nav> 요소
인라인 요소= 한줄에 차례로 배치 된다. 현재 줄에서 필요한 만큼의 너비만을 차지한다.
<a>, <img>, <strong>, <em>, <br>, <input>, <span> 

정적 위치 설정 (static positioning) 정상적인 흐름에 따른 배치
-왼쪽 위로부터 차례로 위에서 아래로 쌓이게된다. Top,bottom,left,right속성의 영향을 받지 않는다.
상대 위치 설정 (relative positioning) 정상적인위치가기준점이된다. 
-정상적인 위치(원래위치)에서 상대적으로 요소가 배치
.
절대 위치 설정 (absolute positioning) 컨테이너의 원점이 기준점이 된다 .
-전체 페이지를 기준으로 시작위치에서 top, left, bottom, right 만큼 떨어진 위치에 배치

고정 위치 설정 (fixed positioning) 윈도우의 원점이 기준점이 된다 .
-브라우저 윈도우에 상대적으로 요소의 위치를 잡는것
-스크롤해도 움직이지 않는다

시맨틱 요소
--과제 보기

Chapt7
자바스크립트: 동적인 웹 페이지를 작성하기 위하여 사용되는 언어
-인터프리트 언어
-동적 타이핑
-구조적 프로그래밍 지원
-객체 기반
- 함수형 프로그래밍 지원

2. 자바스크립트의 6가지 데이터형에 대해 적으시오.(6점)	
-수치형(number), 문자열(string), 부울형(Boolean),객체형(object), undefined,null



8
자바스크립트에서 사용되는 객체를 크게 2가지로 나누면 내장 객체와 사용 자 정의 객체로 나눌 수 있다. ⚫ 내장 객체(built-in object): 자바스크립트에 내장된 객체이다. Date, String, Array와 같은 객체들이 내장 객체이다. ⚫ 사용자 정의 객체(custom object): 개발자가 정의하여 사용하는 객체이다. ⚫ 내장 객체는 다시 핵심 객체와 DOM, BOM 객체로 나눌 수도 있다. ⚫ 핵심 객체: 기본적인 객체인 Date, String, Array들이 핵심 객체이다. ⚫ DOM(Document Object Model): 브라우저가 HTML 문서를 파싱하여서 각 요소들을 객체 트리로 정의한 것이다. ⚫ BOM(Brower Object Model): 브라우저의 종류나 크기 등의 정보를 제공 하는 객체들이다.

이벤트를 처리하는 코드를 버튼에 등록하여야 한다. 이벤트를 처리하는 코 드를 이벤트 핸들러, 또는 이벤트 리스너라고 한다

Chapt9
6. 브라우저 제공 객체 중 window 객체가 무엇인지 간략히 설명하시오.(3점)
window 객체는 BOM에서 최상위 객체로서 웹브라우저 윈도우를 나타내고 있다. 자바스크립트의 모든 전역 함수는 window 객체의 메소드이다

